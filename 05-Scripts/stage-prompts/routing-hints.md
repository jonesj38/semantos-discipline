Using /home/jake/.edwinpai/disciplines/semantos/01-Methodology/evidence-rules.md and existing focused artifacts if available, create Semantos discipline routing hints for future agents.

Produce:
- useWhen and avoidWhen guidance;
- which artifact to consult for which task;
- when to fall back to raw sources-code;
- when docs are acceptable vs when code/proofs/tests/config are required;
- concise YAML snippets for discipline.yaml.
