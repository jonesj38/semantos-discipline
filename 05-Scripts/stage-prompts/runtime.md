Using /home/jake/.edwinpai/disciplines/semantos/01-Methodology/evidence-rules.md, recursively analyze Semantos runtime concepts and execution/data flow from semantos-core source.

Primary evidence: runtime/, core/cell*, cartridges/, db/, configs/, scripts, tests. Docs are secondary only.

Produce a source-grounded artifact with:
- runtime entrypoints/services/components;
- execution/cell/data-flow model as represented in code;
- concrete paths/symbols/commands/tests;
- Mermaid data/control-flow diagram;
- future-agent cautions and unknowns.

Stay in the runtime lane; do not fully analyze formal methods, storage, or developer workflow except as needed for runtime context.
