Using /home/jake/.edwinpai/disciplines/semantos/01-Methodology/evidence-rules.md, recursively analyze Semantos developer workflows from semantos-core source.

Primary evidence: package.json, pnpm/mise/nvm/tsconfig, Docker, systemd, scripts, CI, tests. Docs are secondary only.

Produce a source-grounded artifact with:
- build/test/dev/deploy commands supported by manifests/scripts;
- workspace/package structure;
- local and deployment workflow notes;
- concrete paths/commands;
- future-agent pitfalls.

Stay in the developer workflow lane.
